<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
          http://www.liquibase.org/xml/ns/dbchangelog
          http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.1.xsd">

    <changeSet id="28" author="tijana">
        <createTable tableName="insights_saved_view">
            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="user_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>

            <column name="name" type="VARCHAR(128)">
                <constraints nullable="false"/>
            </column>

            <column name="description" type="VARCHAR(512)"/>

            <column name="provider" type="VARCHAR(32)">
                <constraints nullable="false"/>
            </column>

            <column name="view_config" type="JSONB">
                <constraints nullable="false"/>
            </column>

            <column name="pinned" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>

            <column name="created_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="updated_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addForeignKeyConstraint
                baseTableName="insights_saved_view"
                baseColumnNames="user_id"
                referencedTableName="users"
                referencedColumnNames="id"
                constraintName="fk_insights_saved_view_user"
                onDelete="CASCADE"/>

        <addUniqueConstraint
                tableName="insights_saved_view"
                constraintName="uk_insights_saved_view_user_name"
                columnNames="user_id,name"/>

        <createIndex tableName="insights_saved_view" indexName="idx_insights_saved_view_user">
            <column name="user_id"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
